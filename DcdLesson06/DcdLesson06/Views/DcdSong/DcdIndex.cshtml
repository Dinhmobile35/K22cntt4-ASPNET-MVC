@model IEnumerable<DcdLesson06.Models.DcdSong>

@{
    ViewBag.Title = "Dcd: Danh sách bài hát";
}

<h2>Dcd: Danh sách bài hát</h2>

<p>
    @Html.ActionLink("Create New", "DcdCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DcdTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DcdAuthor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DcdArtist)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DcdYearRelease)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DcdTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DcdAuthor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DcdArtist)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DcdYearRelease)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
