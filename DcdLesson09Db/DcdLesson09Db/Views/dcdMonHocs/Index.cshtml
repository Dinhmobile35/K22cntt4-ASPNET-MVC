@model IEnumerable<DcdLesson09Db.Models.dcdMonHoc>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DcdTenMH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DcdTenMH)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DcdMaMH }) |
            @Html.ActionLink("Details", "Details", new { id=item.DcdMaMH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DcdMaMH })
        </td>
    </tr>
}

</table>
