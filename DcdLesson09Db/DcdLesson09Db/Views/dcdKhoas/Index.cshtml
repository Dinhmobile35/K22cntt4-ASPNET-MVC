@model IEnumerable<DcdLesson09Db.Models.dcdKhoa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DcdTenKH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DcdTenKH)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DcdMaKH }) |
            @Html.ActionLink("Details", "Details", new { id=item.DcdMaKH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DcdMaKH })
        </td>
    </tr>
}

</table>
